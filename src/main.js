// main.js
// Stacks v0.1.0
// Entry point. Initializes the app on page load.
// All modules are loaded via script tags in index.html before this file.

document.addEventListener("DOMContentLoaded", () => {
  init();
});
